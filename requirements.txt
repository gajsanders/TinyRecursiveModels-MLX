mlx>=0.1.0,<1.0.0  # MLX framework (pin a conservative range; CI may override to local shim)
numpy>=1.25,<2.0   # Numerical computations: pinned to 1.25.x for Apple Silicon compatibility
pytest>=7.4,<8.0   # Test runner
black>=24.3,<25.0  # Code formatter
isort>=5.12,<6.0   # Import sorter
flake8>=6.0,<7.0   # Linter
mypy>=1.9,<2.0     # Static type checker

# Optional / runtime packages (left mostly unpinned to avoid heavy CI downloads conflicts)
torch               # Keep for potential legacy compatibility (pin manually if required)
adam-atan2
einops
tqdm
coolname
pydantic
argdantic
wandb
omegaconf
hydra-core
huggingface_hub
packaging
ninja
wheel
setuptools
setuptools-scm
pydantic-core
numba
# triton - optional: not available as a universal wheel on many platforms / Python versions.
# If you need Triton, install it separately following Triton's platform-specific instructions
# (or use a CUDA/Linux environment that provides compatible wheels). Keeping it here
# causes `pip install -r requirements.txt` to fail on many macOS/ARM and older Python versions.
# triton